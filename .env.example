# Copy this file to .env (which is gitignored) and adjust if your backend
# runs on a different port. Vite only exposes env vars prefixed with VITE_
# to the browser — this is a Vite requirement, not a project choice.
VITE_API_BASE_URL=http://localhost:4000/api/v1
